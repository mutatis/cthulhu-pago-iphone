using UnityEngine;
using System.Collections;

public class ForceToCanvas : MonoBehaviour
{
	/*RigidBody2DUnidirectional rigidBody2DUnidirectional;

	void Start ()
	{
		InvokeRepeating("ContrainByForce",1,1);
	}
	// Update is called once per frame
	void Update ()
	{
		if (rigidBody2DUnidirectional.axisToMove == Axis.y && transform.position.y >  rigidBody2DUnidirectional.upperLimit || transform.position.y >  rigidBody2DUnidirectional.upperLimit
	}*/
}
