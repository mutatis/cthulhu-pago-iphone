using UnityEngine;
using System.Collections;

public class PowerUpGenerator : MonoBehaviour
{
    //private GameObject magneticPrefab;

    /*if (GameManager.gameManager.hasMagnetic)
    {
        magneticPrefab = (GameObject)Resources.Load("Magnetic", typeof(GameObject));
        magneticPrefab = Instantiate(magneticPrefab, transform.position + new Vector3(0,4f,0), magneticPrefab.transform.rotation) as GameObject;
        magneticPrefab.transform.parent = transform;
        StartCoroutine("UsingMagnetic", 60);
    }*/
	// Use this for initialization
	void Start ()
    {
	
	}
	
	// Update is called once per frame
	void Update ()
    {
	
	}
}
