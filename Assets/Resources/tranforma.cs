using UnityEngine;
using System.Collections;

public class tranforma : MonoBehaviour {

	// Use this for initialization
	void Start () {
		transform.position = new Vector2(transform.position.x - 1500, transform.position.y);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
